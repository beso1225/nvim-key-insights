{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "suggestions"],
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "suggestions": {
      "type": "array",
      "maxItems": 100,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["action", "title", "rationale", "mapping", "evidence", "collision_check"],
        "properties": {
          "action": { "type": "string", "enum": ["learn_existing", "add_mapping", "change_mapping", "no_change"] },
          "title": { "type": "string", "minLength": 1, "maxLength": 256 },
          "rationale": { "type": "string", "minLength": 1, "maxLength": 4096 },
          "mapping": {
            "type": ["object", "null"],
            "additionalProperties": false,
            "required": ["mode", "scope", "lhs", "target_mapping_id"],
            "properties": {
              "mode": { "type": "string", "enum": ["normal", "visual", "operator_pending"] },
              "scope": { "type": "string", "enum": ["global", "buffer"] },
              "lhs": {
                "type": "array",
                "minItems": 1,
                "maxItems": 64,
                "items": { "type": "string", "minLength": 1, "maxLength": 64 }
              },
              "target_mapping_id": {
                "type": ["string", "null"],
                "pattern": "^mapping-v1:[0-9a-f]{64}$"
              }
            }
          },
          "evidence": {
            "type": "array",
            "minItems": 1,
            "maxItems": 32,
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["metric", "value"],
              "properties": {
                "metric": {
                  "type": "string",
                  "enum": [
                    "sessions",
                    "events",
                    "total_session_duration_ms",
                    "key_sequences",
                    "sequence_keys",
                    "text_runs",
                    "text_keys",
                    "mode_transitions",
                    "mapping_uses",
                    "repeated_key_runs",
                    "repeated_key_presses",
                    "unique_keys",
                    "unique_mappings",
                    "unique_repeated_keys",
                    "observed_mappings",
                    "unobserved_mappings",
                    "total_snapshot_mappings",
                    "count_prefix_occurrences",
                    "count_prefix_digit_presses"
                  ]
                },
                "value": { "type": "integer", "minimum": 0 }
              }
            }
          },
          "collision_check": {
            "type": "object",
            "additionalProperties": false,
            "required": ["checked", "conflicting_mapping_ids"],
            "properties": {
              "checked": { "type": "boolean", "const": true },
              "conflicting_mapping_ids": {
                "type": "array",
                "maxItems": 4096,
                "items": {
                  "type": "string",
                  "pattern": "^mapping-v1:[0-9a-f]{64}$"
                }
              }
            }
          }
        }
      }
    }
  }
}
